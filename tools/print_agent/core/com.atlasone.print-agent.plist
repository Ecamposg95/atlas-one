<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<!--
  Plantilla del LaunchAgent del Atlas Print Agent.

  NO es el archivo instalado: core/instalar-servicio-mac.sh sustituye los
  cuatro marcadores de posición (destino, orígenes, puerto y carpeta de logs)
  y escribe el resultado en ~/Library/LaunchAgents/. Editar a mano el archivo
  instalado no sirve de nada: la siguiente instalación lo sobrescribe.

  (Este comentario no nombra los marcadores literalmente a propósito: la
  sustitución es textual y los reemplazaría también aquí.)

  Es un LaunchAgent de USUARIO, no un LaunchDaemon: arranca al iniciar sesión,
  no necesita sudo y ve la misma cola de CUPS que el usuario de la caja.
-->
<plist version="1.0">
<dict>
  <key>Label</key><string>com.atlasone.print-agent</string>
  <key>ProgramArguments</key>
  <array>
    <string>{WORKDIR}/venv/bin/python3</string>
    <string>{WORKDIR}/main.py</string>
  </array>
  <key>WorkingDirectory</key><string>{WORKDIR}</string>
  <key>EnvironmentVariables</key>
  <dict>
    <key>ATLAS_AGENT_HOST</key><string>127.0.0.1</string>
    <key>ATLAS_AGENT_PORT</key><string>{PORT}</string>
    <!-- Dominios del POS para el preflight CORS del agente. El regex de
         fábrica ya cubre localhost, *.up.railway.app y (*.)atlasone.com.mx;
         esto es el refuerzo explícito y la única vía para un dominio propio
         distinto. Sin el dominio correcto la caja VENDE PERO NO IMPRIME. -->
    <key>ATLAS_AGENT_ORIGINS</key><string>{ORIGINS}</string>
    <!-- launchd no hereda el PATH de la sesión. /usr/bin y /usr/sbin son
         obligatorios: el agente llama lp, lpstat, lpadmin y lpinfo. -->
    <key>PATH</key><string>/usr/bin:/bin:/usr/sbin:/sbin:/usr/local/bin</string>
  </dict>
  <key>RunAtLoad</key><true/>
  <key>KeepAlive</key><true/>
  <key>ThrottleInterval</key><integer>5</integer>
  <key>StandardOutPath</key><string>{LOGDIR}/agent.out.log</string>
  <key>StandardErrorPath</key><string>{LOGDIR}/agent.err.log</string>
</dict>
</plist>
